export enum AuthorizationEnum {
    READONLY = 0,
    UPDATE = 1,
    PENDING = 2,
    WAITING = 3,
    FRIEND = 4
}

export enum UserStatusEnum {
    DISABLED = 0,
    CREATED = 1,
    VERIFIED = 2,
    ADMIN = 3
}

export enum ClubTypeEnum {
    NIGHTCLUB = 0,
    BAR = 1
}